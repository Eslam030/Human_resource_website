{
    "builds": [{
        "src": "Human_resource_server/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "Human_resource_server/wsgi.py"
        }
    ]
}